const fs = require('fs');

function findNiceStrings(){
    let vowels = ['a', 'e', 'i', 'o', 'u'];
    let badStrings = ['ab', 'cd', 'pq', 'xy'];

    let niceStrings = 0;
    
}









fs.readFile('./file.txt', 'utf8', (err, data) => {
    if(err){
        console.log(err);
        return;
    }
})
